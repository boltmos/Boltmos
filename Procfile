web: python backend/main.py
# NOTE: this Procfile is for LOCAL use only (backend/main.py needs pyautogui,
# a real display, and Windows app paths to do desktop automation - it will
# not run on Railway or any headless Linux host).
#
# The Railway-deployable service lives in backend/cloud/ (Groq + Supabase
# only, no desktop automation). Set Railway's service Root Directory to
# "backend/cloud" so it picks up backend/cloud/Procfile and
# backend/cloud/requirements.txt automatically - see the deployment notes
# for details.
